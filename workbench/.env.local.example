# Local-only (gitignored). Do NOT commit real keys.
OPENAI_API_KEY=

# Optional: if you want the workbench to fetch apps from a remote index instead of local index.json
# AI_PROMPTS_INDEX_URL=https://raw.githubusercontent.com/<you>/<repo>/main/index.json

